public class BSLogs {
    // create logs for testing purposes
    // some tables should be currently active
    // some tables should be recently inactive - are they still in active tables list?
    // some tables should be inactive for a long time
    // maybe add inexisting tables?
}
